// const hasLowPerformance = () => {
//     const ua = navigator.userAgent;
//     const isLowEndDevice = /iPhone|iPad|iPod|Android/.test(ua) && !/Pixel|Pixel 2|Pixel 3/.test(ua)
//     const isLowMemory = navigator.deviceMemory && navigator.deviceMemory <= 2

//     return isLowEndDevice || isLowMemory
// };

// if (hasLowPerformance()) document.body.classList.add('disable-animations')